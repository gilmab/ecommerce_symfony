{% extends 'base.html.twig' %}

{% block title %} Mes addresse - la bouti{% endblock %}

{% block content %}
        <a href="{{ path('ajout')}}" class="btn btn-info btn-sm float-right">Ajouter une addresse </a>
        <h1> Mes addresses   </h1>
        <p> C'est ici que l'on gérera nos addresses </p>
         <a href="{{ path('compte')}}">Retouner vers le compte  </a>
       <hr> 
  
        {% if app.user.addresses|length == 0 %}
        <p class="text-center">
                Vous n'avez pas d'addresse pour en ajouter veuiller cliquer ici <a href="{{ path('ajout')}}"> cliquer ici</a>
        </p>
        {% else %} 
       
                <div class="row">
                      {% for address in app.user.addresses %}
                    <div class="col-md-4">
                        <div class="card" style="width: 18rem;">
                                <div class="card-body">
                                 <h5 class="card-title"> {{ address.name }}</h5>
                                 <p class="card-text"> 
                                 {{ address.address}}, 
                                 {{ address.postal}} - {{ address.city}} - {{address.country}}
                                 </p>
                                 <a href="{{ path('update_address', {'id': address.id})}}" class="btn btn-primary"> Modifier</a> | <a href="{{ path('delete_address', {'id': address.id})}}" class="btn btn-primary"> Supprimer</a>
                                </div>
                           </div>
                      </div>
                      {% endfor %}
                </div>

         

        {% endif %}
{% endblock %}
